import Loading from "../loading"
import './index.css'

const LoadingOverlay = () => {
    return <div className="loading-overlay-container">
        <Loading />
    </div>
}

export default LoadingOverlay